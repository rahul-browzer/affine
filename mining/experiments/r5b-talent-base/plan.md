# R5b — Talent reign-base full-FT (non-king-2)

## Axis
Dense thought-only full-FT on **TalentPigs** reign-3 weights (not Tok, not
Genesis). Same `winner_za_high_l1` data as R4/R5. Isolates base: R5=Genesis,
R5b=Talent. Structural — not a cosmetic parent swap.

## Pod
`mine-r5-nonking-2` via fleet-rent (queue after R4b). 8×B300 prefer. TTL 24h.
Uploader: `upload_and_launch.sh` (fleet-boot case, pass 2081).

## Decision
n80 vs Tok; submit only if hr ≥ 1.5×(k_sigma·SE), live k=2.0.
Watch: `/root/logs/r5b_pipeline.nohup` / `h122_train.nohup`.
