# pass218 — dual-sim race kill

Same race as H42: post_train sim16406 + retry sim16585 dual-writing.
Killed both + post_train; quarantine progress. Retry attempt 2/3
`block_hash=b203…` single sim. post_train patched (watcher-armed skip).
